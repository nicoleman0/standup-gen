[project]
name = "standup-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "groq>=1.2.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
standup = "standup.standup:main"
